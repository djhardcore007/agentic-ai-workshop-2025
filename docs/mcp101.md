# Questions
## 1. what LLM can do? 
- 1.1 language model: complete a sentence
`<tok1><tok2><tok3><tok4>`
- 1.2 tool calls? how can LLM understand what functions to use?
hardcoded: `<tok1><tok2>...<tok3><tok4>`
tool calls:<tok1><tok2><func_name><input><output><tok3><tok4>

this turns llm into "agent" that can orchestrate workflow
## 2. what are trditional workflow like?
- DAG: workflow 
    - DAG is fixed. so once u set up a workflow. u cant change it.
    - trading strategy. fixed
- how can DAG be more flexible?
- agentic DAG
    - (a) --> (b) --> (c)
    - a/b/c can be anything.
## 3. what is MCP? why ppl need MCP?
- LLM is trained using its own schema.
- all tools functions / API (gRPC (RESTFUL)) but not all APIs follow this pattern.
- MCP SERVER
- serve ur codebase into a standardized protocols where it matches how tools/funcs are defined in LLM training.

in production, companies have:
- databade
    - relational(sql): TABLE -> postgre, combdb, sqllite, csv, excel
    - non-relational(nosql): KEY-VAL. s3, gcp-bucket
    - graph: knowledge graph, neo4j, social network.
- codebase
    - github.

host ur codebase as a API function calls. so LLM can read the API doc. and then learn to use it.

in the cloud, AI labs have
- LLM1. OpenAI. CHATGPT 5.2
- LLM2. claude-opus 4.5
- LLM3. deepseek

ppl want a unified / standard way to use the database / codebase.

1. wrap the database into some sort where all LLMs (whatever the labs, models) can use in the same way.
--> VECTOR DB.  RAG
--> how to turn a traditional db into a vector db?
    -> what is traditional db? 
        - time series. number.
        - categorical db
        - textual db. --> embedding. how to turn 10k document --> vector?
        -> 10k docuemtn --> chunking. (1 sentence / 10 sentences / 1 paragraph)
        -> embedding. open-source. qwen smaller models.
        --> chunk --> sentence emb.
        --> indexes of vectors.
    -> vector db? a list of vector. each vector has some meta. (locatino of the chunk)
    -> how to turn? embedding. open-source. qwen smaller models.

2. wrap the codebase into some sort where all LLMs (whatever the labs, models) can use in the same way.
- MCP server
- turn a codebase (a list of functions/ methods) into a way where LLM can use it.
?
- wrappers
- @fastmcp. decorator on top of a function
```
@fastmcp.register("func_1")
def func_1(args):
    pass
```

codebase --> MCP server (localhost:8080/func2)

LLM_client (openai api)
```
import openai

llm = openai.ChatCompletion.create(
    model="gpt-3.5-turbo",
    messages=["dfsdfjsf s "]
    mcp_server="localhost:8080"
) # client
```
Now we have codebase?
data sports API

1. Read the API docs.
API 100 functions. 
context mangement.
2. turn the API functions calls into MCP servers.
I need a MCP server where all the API calls are registere in the MCP server.
3. u need to pass the MCP servers to LLM.
